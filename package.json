{
  "name": "disgrowth",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MARKET GAME — STORE. Real-money store for Disgrowth, the Discord economy game.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test tests/*.test.js",
    "migrate": "node src/migrate.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "express-rate-limit": "^7.5.0",
    "helmet": "^8.1.0",
    "pg": "^8.14.1"
  }
}
